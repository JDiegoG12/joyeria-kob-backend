import { Category } from '@prisma/client';

/**
 * Alias de dominio para el modelo `Category` generado por Prisma.
 * Permite que el resto del módulo dependa de este tipo en lugar del cliente.
 */
export type ICategory = Category;
